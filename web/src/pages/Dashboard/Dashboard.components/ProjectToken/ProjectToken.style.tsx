import styled from 'styled-components/macro'
import { Card, FadeInFromLeft } from 'src/styles'

export const ProjectTokenStyled = styled(FadeInFromLeft)``

export const ProjectTokenCard = styled(Card)`
  margin-bottom: 20px;
  padding: 20px 20px 10px 20px;
  height: 300px;
`

export const ProjectTokenTitle = styled.div`
  text-transform: uppercase;
  font-size: large;
  margin-bottom: 15px;
`
